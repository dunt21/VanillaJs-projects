{
  "name": "weather-app-main",
  "version": "1.0.0",
  "description": "weather app",
  "license": "ISC",
  "author": "suadddd",
  "type": "commonjs",
  "source": "index.html",
  "scripts": {
    "start": "npx @tailwindcss/cli -i ./src/input.css -o ./src/output.css --watch",
    "clean": "rimraf dist .parcel-cache",
    "build": "npx @tailwindcss/cli -i ./src/input.css -o ./src/output.css && parcel build",
    "run": "parcel index.html"
  },
  "dependencies": {
    "@tailwindcss/cli": "^4.1.16",
    "tailwindcss": "^4.1.16"
  },
  "devDependencies": {
    "daisyui": "^5.3.10",
    "parcel": "^2.16.0",
    "rimraf": "^6.1.2",
    "url": "^0.11.4"
  }
}
